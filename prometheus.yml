global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'broker'
    dns_sd_configs:
      - names:
          - 'tasks.broker'
        type: 'A'
        port: 80

  - job_name: 'router'
    dns_sd_configs:
      - names:
          - 'tasks.router'
        type: 'A'
        port: 80

  - job_name: 'syncer'
    dns_sd_configs:
      - names:
          - 'tasks.syncer'
        type: 'A'
        port: 80

  - job_name: 'nginx'
    static_configs:
      - targets: ['nginx']

rule_files:
  - "rules.yml"
